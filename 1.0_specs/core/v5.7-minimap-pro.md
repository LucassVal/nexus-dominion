# SPEC: Nexus Dominion v5.7 — Minimap Pro (WoCC + Industry Patterns)

## Fonte: WoCC rendering engine + Game Minimap Guide (solana.garden)

## PADROES EXTRAIDOS

### WoCC → Nosso Canvas 2D:
- GFX tiers → simplificar: 1 tier (mobile-optimized)
- Splat mapping terrain → nosso terrain cache (tCache) ja faz isso
- Pre-baked texture → reusar tCache escalado para minimapa
- Layer stack → implementar: terrain → fog → cities → entities → player → frustum
- LOD → cidades so aparecem se visitadas ou proximas
- Alpha interpolation → nao precisamos (simulacao ja eh 2D)

### Minimap Guide → Aplicar:
- Pre-baked terrain texture (ja temos!)
- Fog of war: nao visitado = escuro, visitado = visivel
- Layer stack completo
- Icon design: shape + color (nao so cor)
- Frustum indicator (viewport rectangle)
- Click-to-move routing (ja temos)

---

## IMPLEMENTACAO: MINIMAP PRO

### Tamanho: 200x150px (ocupando canto inferior direito)
### Layers (bottom to top):

1. **TERRAIN**: tCache escalado para 200x150 via drawImage com scale
2. **FOG**: overlay preto semi-transparente sobre areas nao visitadas
3. **CITIES**: dots coloridos (tamanho por tipo), com borda branca
4. **ENTITIES**: bandits (X vermelho), traders ($ verde), armies (escudo)
5. **PLAYER**: triangulo dourado com anel pulsante + label "VOCE"
6. **FRUSTUM**: retangulo da viewport atual
7. **COMPASS**: "N" no topo (norte sempre pra cima)

### Performance:
- Terrain: drawImage do tCache (1 operacao, nao redesenha tile por tile)
- Entities: so desenha se dentro da viewport do jogador (culling)
- Update: todo frame (leve, so algumas operacoes de canvas)

### Fog of War:
- Cidades nao visitadas: cinza escuro
- Terreno nao explorado: overlay preto 60%
- Conforme player visita, revela (permanente)

---

## IMPLEMENTACAO (30 linhas)
1. Aumentar canvas: 170x120 → 200x150
2. Usar ctx.drawImage(tCache, ...) para terrain (1 chamada!)
3. Adicionar fog overlay
4. Melhorar icons por tipo de cidade
5. Compass "N"
